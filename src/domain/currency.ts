/** Single marketplace currency — Georgian Lari. Prices stored as tetri (1/100 ₾). */
export const APP_CURRENCY = "GEL" as const;

/** Browse chip: show listings under this major-unit price. */
export const BROWSE_MAX_PRICE_GEL = 20;
