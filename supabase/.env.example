# Local Google OAuth testing only (see docs/provisioning.md § Google sign-in).
# Copy to supabase/.env, fill in real values, then run:
#   supabase start --env-file supabase/.env
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=
SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET=
